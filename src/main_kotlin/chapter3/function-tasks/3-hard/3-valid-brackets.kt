/*
*
* 🔴 Task 3: Valid Brackets
Description:
Write a function isValid that checks whether a string containing only ()[]{} is valid. A string is valid if brackets are closed in the correct order.

kotlin
Copy
Edit
fun isValid(s: String): Boolean {
    // Your implementation
}
Example:

kotlin
Copy
Edit
isValid("({[]})") // returns true
isValid("({[})")  // returns false
*
* */